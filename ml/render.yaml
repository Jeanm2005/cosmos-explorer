services:
  - type: web
    name: stellar-host-api
    runtime: python
    rootDir: ml
    buildCommand: pip install -r api/requirements.txt
    startCommand: uvicorn api.main:app --host 0.0.0.0 --port $PORT
    plan: free